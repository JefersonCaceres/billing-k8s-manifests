apiVersion: v1
kind: Service
metadata:
  name: billing-backend-service
spec:
  type: LoadBalancer
  selector:
    app: billing-backend
  ports:
    - port: 8080
      targetPort: 8080
  type: LoadBalancer
